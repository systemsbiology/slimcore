<h1>Listing lab_memberships</h1>

<table>
  <tr>
  </tr>

<% @lab_memberships.each do |lab_membership| %>
  <tr>
    <td><%= link_to 'Show', lab_membership %></td>
    <td><%= link_to 'Edit', edit_lab_membership_path(lab_membership) %></td>
    <td><%= link_to 'Destroy', lab_membership, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New lab_membership', new_lab_membership_path %>